<%= form_with(model: menu, local: true) do |form| %>
  <% if menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu.errors.count, "error") %> prohibited this menu from being saved:</h2>

      <ul>
      <% menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :discountClass %>
    <%= form.number_field :discountClass %>
  </div>

  <div class="field">
    <%= form.label :articles_id %>
    <%= form.text_field :articles_id %>
  </div>

  <div class="field">
    <%= form.label :productable_id %>
    <%= form.text_field :productable_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
